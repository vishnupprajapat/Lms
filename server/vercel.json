{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    }
  ],
    "functions": {
    "api/login": {
      "maxDuration": 20
    },
    "api/register": {
      "maxDuration": 20
    }
  }
  "routes": [{ "src": "/(.*)", "dest": "server.ts" }]
}
